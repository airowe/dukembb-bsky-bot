{
  "name": "dukembb-bsky-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:worker": "vitest run worker/tests",
    "deploy:worker": "wrangler deploy --config worker/wrangler.toml"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250229.0",
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^2.1.8",
    "wrangler": "^3.112.0"
  }
}
